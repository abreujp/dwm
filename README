dwm - dynamic window manager
============================
dwm is an extremely fast, small, and dynamic window manager for X.

Requirements
------------
sudo apt install build-essential libtool autoconf automake pkg-config git
sudo apt install libx11-dev libxinerama-dev libxft-dev
sudo apt install slock playerctl scrot feh pulseaudio-utils libxcb-keysyms1-dev
sudo apt install libnss3-tools


